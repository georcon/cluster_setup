apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: keycloak
spec:
  tls:
    - hosts:
      - keycloak.etds.local
  rules:
  - host: keycloak.etds.local
    http:
      paths:
      - backend:
          serviceName: keycloak
          servicePort: 8080

