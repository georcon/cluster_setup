apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: jira
spec:
  tls:
    - hosts:
      - jira.etds.me
  rules:
  - host: jira.etds.me
    http:
      paths:
      - backend:
          serviceName: jira
          servicePort: 8080

