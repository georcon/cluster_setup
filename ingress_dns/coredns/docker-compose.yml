version: '3'
services:
  web:
    image: coredns/coredns
    ports:
      - "10.0.110.0:53:53/udp"
        #networks:
        #- coredns_net
    volumes:
            - ./config/:/root/
    command:
            -conf /root/Corefile
    restart: always

    #networks: 
    #coredns_net:
